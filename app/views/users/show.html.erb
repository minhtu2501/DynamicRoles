<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<table class="table table-striped">
	<tr>
		<th> Role Name</th>
	</tr>
	<% @roles.each do |role|%>
	<tr>
		<td><%= role.name %></td>
	</tr>
	<% end %>
</table>
<br />
<p>Permission</p>
<table class="table table-striped">
	<tr>
		<th> Subject Class</th>
		<th> Action </th>
	</tr>
	<% @permissions.each do |per|%>
	<tr>
		<td><%= per.subject_class %></td>
		<th><%= per.action %></th>
		<td></td>
	</tr>
	<% end %>
</table>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Add Permission', edit_permissions_user_path(@user)%> |
<%= link_to 'Reset Permission', reset_permissions_user_path(@user), method: :put %> |
<%= link_to 'Back', users_path %>
